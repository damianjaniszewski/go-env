applications:
- name: go-env
  buildpack: https://github.com/cloudfoundry/go-buildpack#v1.8.3
  memory: 32M
  instances: 1
  # services:
    # db: mongodb
    # queue: rabbitmq3
  stackato:
    description: Print container config variables
